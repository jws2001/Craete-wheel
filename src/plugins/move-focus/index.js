export default class MoveFocus {
    constructor(options = {
        index: 0, // 当前激活下标
        indexList: [], // key
        scollWrapper: null, // 滚动容器
        scollItem: null, // 滚动元素
    }) {

    }
}